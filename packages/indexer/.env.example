# ── Database ─────────────────────────────────────────────────────────────────
# Local development: Use SQLite (override the schema locally if needed)
# Production: Use Neon.tech (free tier) or Railway PostgreSQL
DATABASE_URL="postgresql://user:password@host:5432/defi_yield_vault?sslmode=require"

# ── Blockchain RPC ────────────────────────────────────────────────────────────
# Local: Hardhat node (no key needed)
# Production: Get from Alchemy (free tier) https://alchemy.com
RPC_URL="http://127.0.0.1:8545"

# ── Contract Address ──────────────────────────────────────────────────────────
# Update this after deploying to Sepolia
CONTRACT_ADDRESS="0x5fbdb2315678afecb367f032d93f642f64180aa3"
