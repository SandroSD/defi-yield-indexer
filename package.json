{
  "name": "defi-yield-portfolio",
  "private": true,
  "description": "Professional DeFi Yield Indexer & Analytics Portfolio",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "prettier": "^3.0.0"
  }
}
